[project]
name = "openclaw-xhs-workflow"
version = "0.1.0"
description = "OpenClaw plugin for XiaoHongShu automation, AI content workflow, image generation, and draft-first publishing"
requires-python = ">=3.9"

[tool.setuptools]
py-modules = []
